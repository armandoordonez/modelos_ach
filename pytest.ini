[pytest]
testpaths = tests
pythonpath = jobs
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
markers =
    parity: compara metricas contra los notebooks originales; requiere el dataset curado
    integration: requiere MinIO o Docker levantados
